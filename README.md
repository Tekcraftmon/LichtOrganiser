# LichtOrganiser
LichtOrganiser is a lightweight cross-platform utility that cleans up cluttered Downloads and Documents folders by organising existing files based on their file extensions. It automatically creates extension folders, prevents duplicate file overwrites, and includes a safe reset feature.
